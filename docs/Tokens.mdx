import { Meta } from '@storybook/blocks';

<Meta title="Design Tokens" />

# Design Tokens

Design tokens are the visual design atoms of the design system. They are named entities that store visual design attributes.

## Colors

### Primary Colors
Used for primary actions and emphasis.

<div style={{ display: 'grid', gridTemplateColumns: 'repeat(auto-fill, minmax(120px, 1fr))', gap: '1rem', marginBottom: '2rem' }}>
  {[50, 100, 200, 300, 400, 500, 600, 700, 800, 900].map(shade => (
    <div key={shade}>
      <div style={{ 
        backgroundColor: `var(--color-primary-${shade})`, 
        height: '80px', 
        borderRadius: '8px',
        marginBottom: '0.5rem',
        border: '1px solid rgba(0,0,0,0.1)'
      }} />
      <div style={{ fontSize: '0.875rem' }}>
        <strong>{shade}</strong>
        <br />
        <code style={{ fontSize: '0.75rem' }}>--color-primary-{shade}</code>
      </div>
    </div>
  ))}
</div>

### Neutral Colors
Used for text, backgrounds, and borders.

<div style={{ display: 'grid', gridTemplateColumns: 'repeat(auto-fill, minmax(120px, 1fr))', gap: '1rem', marginBottom: '2rem' }}>
  {[50, 100, 200, 300, 400, 500, 600, 700, 800, 900].map(shade => (
    <div key={shade}>
      <div style={{ 
        backgroundColor: `var(--color-neutral-${shade})`, 
        height: '80px', 
        borderRadius: '8px',
        marginBottom: '0.5rem',
        border: '1px solid rgba(0,0,0,0.1)'
      }} />
      <div style={{ fontSize: '0.875rem' }}>
        <strong>{shade}</strong>
        <br />
        <code style={{ fontSize: '0.75rem' }}>--color-neutral-{shade}</code>
      </div>
    </div>
  ))}
</div>

### Semantic Colors

<div style={{ display: 'grid', gridTemplateColumns: 'repeat(3, 1fr)', gap: '1rem', marginBottom: '2rem' }}>
  <div>
    <h4>Success</h4>
    <div style={{ 
      backgroundColor: 'var(--color-success-500)', 
      height: '80px', 
      borderRadius: '8px',
      marginBottom: '0.5rem'
    }} />
    <code style={{ fontSize: '0.75rem' }}>--color-success-500</code>
  </div>
  <div>
    <h4>Warning</h4>
    <div style={{ 
      backgroundColor: 'var(--color-warning-500)', 
      height: '80px', 
      borderRadius: '8px',
      marginBottom: '0.5rem'
    }} />
    <code style={{ fontSize: '0.75rem' }}>--color-warning-500</code>
  </div>
  <div>
    <h4>Error</h4>
    <div style={{ 
      backgroundColor: 'var(--color-error-500)', 
      height: '80px', 
      borderRadius: '8px',
      marginBottom: '0.5rem'
    }} />
    <code style={{ fontSize: '0.75rem' }}>--color-error-500</code>
  </div>
</div>

## Typography

### Font Sizes

<div style={{ marginBottom: '2rem' }}>
  {[
    { name: 'xs', size: '0.75rem' },
    { name: 'sm', size: '0.875rem' },
    { name: 'base', size: '1rem' },
    { name: 'lg', size: '1.125rem' },
    { name: 'xl', size: '1.25rem' },
    { name: '2xl', size: '1.5rem' },
    { name: '3xl', size: '1.875rem' },
    { name: '4xl', size: '2.25rem' },
    { name: '5xl', size: '3rem' }
  ].map(({ name, size }) => (
    <div key={name} style={{ marginBottom: '1rem' }}>
      <div style={{ fontSize: `var(--font-size-${name})` }}>
        The quick brown fox jumps over the lazy dog
      </div>
      <code style={{ fontSize: '0.75rem', color: 'var(--color-neutral-600)' }}>
        --font-size-{name} ({size})
      </code>
    </div>
  ))}
</div>

### Font Weights

<div style={{ marginBottom: '2rem' }}>
  {[
    { name: 'normal', weight: '400' },
    { name: 'medium', weight: '500' },
    { name: 'semibold', weight: '600' },
    { name: 'bold', weight: '700' }
  ].map(({ name, weight }) => (
    <div key={name} style={{ marginBottom: '1rem' }}>
      <div style={{ fontWeight: `var(--font-weight-${name})`, fontSize: '1.125rem' }}>
        The quick brown fox jumps over the lazy dog
      </div>
      <code style={{ fontSize: '0.75rem', color: 'var(--color-neutral-600)' }}>
        --font-weight-{name} ({weight})
      </code>
    </div>
  ))}
</div>

## Spacing

Our spacing scale is based on a 4px (0.25rem) base unit.

<div style={{ marginBottom: '2rem' }}>
  {[
    { name: '1', value: '0.25rem', px: '4px' },
    { name: '2', value: '0.5rem', px: '8px' },
    { name: '3', value: '0.75rem', px: '12px' },
    { name: '4', value: '1rem', px: '16px' },
    { name: '5', value: '1.25rem', px: '20px' },
    { name: '6', value: '1.5rem', px: '24px' },
    { name: '8', value: '2rem', px: '32px' },
    { name: '10', value: '2.5rem', px: '40px' },
    { name: '12', value: '3rem', px: '48px' },
    { name: '16', value: '4rem', px: '64px' }
  ].map(({ name, value, px }) => (
    <div key={name} style={{ display: 'flex', alignItems: 'center', marginBottom: '0.75rem' }}>
      <div style={{ 
        width: value, 
        height: '24px', 
        backgroundColor: 'var(--color-primary-500)',
        marginRight: '1rem',
        borderRadius: '4px'
      }} />
      <code style={{ fontSize: '0.875rem' }}>
        --spacing-{name} = {value} ({px})
      </code>
    </div>
  ))}
</div>

## Border Radius

<div style={{ display: 'grid', gridTemplateColumns: 'repeat(auto-fill, minmax(150px, 1fr))', gap: '1rem', marginBottom: '2rem' }}>
  {[
    { name: 'none', value: '0' },
    { name: 'sm', value: '0.125rem' },
    { name: 'base', value: '0.25rem' },
    { name: 'md', value: '0.375rem' },
    { name: 'lg', value: '0.5rem' },
    { name: 'xl', value: '0.75rem' },
    { name: '2xl', value: '1rem' },
    { name: 'full', value: '9999px' }
  ].map(({ name, value }) => (
    <div key={name}>
      <div style={{ 
        width: '100px', 
        height: '100px', 
        backgroundColor: 'var(--color-primary-500)',
        borderRadius: `var(--border-radius-${name})`,
        marginBottom: '0.5rem'
      }} />
      <div style={{ fontSize: '0.875rem' }}>
        <strong>{name}</strong>
        <br />
        <code style={{ fontSize: '0.75rem' }}>--border-radius-{name}</code>
        <br />
        <span style={{ fontSize: '0.75rem', color: 'var(--color-neutral-600)' }}>{value}</span>
      </div>
    </div>
  ))}
</div>

## Usage

### In CSS

```css
.my-component {
  color: var(--color-primary-500);
  padding: var(--spacing-4);
  border-radius: var(--border-radius-md);
  font-size: var(--font-size-base);
}
```

### In JavaScript

```tsx
const styles = {
  backgroundColor: 'var(--color-primary-500)',
  padding: 'var(--spacing-4)',
  borderRadius: 'var(--border-radius-md)',
};
```

## Customization

Override tokens by defining your own CSS custom properties:

```css
:root {
  --color-primary-500: #your-brand-color;
  --font-family-sans: 'Your Font', sans-serif;
  --border-radius-md: 8px;
}
```
